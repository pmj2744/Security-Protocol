# 보안프로토콜 기말 프로젝트 뼈대 코드
# [공지] 우리 계획서 흐름을 기반으로 하되, 교수님 강의 자료(4-표준보안프로토콜) 개념을 녹여낸 뼈대 코드입니다. 각자 담당한 함수 안의 'pass'를 지우고 코드를 작성해 주세요! 
# 다른 부분(함수 이름, 괄호 안의 입력값)은 수정할 시 상의 후 변경해 주세요

def create_certificate(user_id):
    """
    [B 조민정 요원] 사용자 등록 및 인증서 발급
    - 계획서 흐름: 회원가입 시 디지털 인증서 발급
    - 강의 자료 연계: 인증서 발급 시 그냥 텍스트 파일이 아니라 표준 규격인 'X.509 v3' 형태로 생성!
    - 가이드: cryptography 라이브러리를 사용해 ECDSA 또는 RSA 키쌍을 만들고 X.509 인증서 파일(.crt)로 저장 권유, 저장 다르게 할 경우 꼭 공유해 주세요.
    """
    print(f"\n[1단계] {user_id} 사용자의 표준 X.509 v3 인증서 및 개인키 생성 중...")
    # 여기에 코드를 채워 넣을 공간
    pass


def sign_login(private_key, challenge):
    """
    [C 정은미] 전자서명 로그인 및 데이터 무결성 검증
    - 계획서 흐름: 사용자가 개인키로 서명하고 서버가 로그인 승인
    - 강의 자료 연계: 로그인 과정을 강의 자료에 나오는 'Challenge-Response(난수 던지고 답하기)' 방식으로 구현!

    - 가이드: 서버가 던진 무작위 난수에 사용자의 개인키로 SHA-256 기반 전자서명을 만들어 응답하는 코드 작성, 서버는 이 서명을 사용자의 공개키로 검증하여 로그인 승인 여부 결정. 로그인 성공 시 '로그인 승인' 메시지 출력, 실패 시 '로그인 거부' 메시지 출력하도록 구현 권유.
    """
    print(f"\n[2단계] 로그인 시도: 서버가 던진 암구호('{challenge}')에 유저 개인키로 전자서명 중...")
    # 여기에 코드를 채워 넣을 공간
    pass


def hide_message(image_path, text):
    """
    [D 유채원 요원] 암호화 통신 및 스테가노그래피 적용
    - 계획서 흐름: 메시지를 암호화하여 이미지 속에 숨겨서 전송
    - 강의 자료 연계: 강의 자료의 'TLS Record 프로토콜(대칭키 암호화 통신)' 개념을 반영!

    - 가이드: 평문 메시지(text)를 속도가 빠른 'AES 대칭키 알고리즘'으로 1차 암호화한 뒤, Pillow 라이브러리로 원본 이미지(image_path)의 LSB(최하위 비트)에 숨기는 코드 작성. 이미지에 메시지를 숨긴 후, '메시지 숨기기 완료' 메시지 출력하도록 구현 권유. 
    """

    print(f"\n[3단계] 안전한 통신: 전송할 메시지를 AES로 암호화한 후 이미지('{image_path}')에 LSB 숨기기 진행 중...")
    # 코드를 채워 넣을 공간
    pass


def verify_otp(user_code):
    """
    [E 김정서 요원] 다중 인증(MFA) 및 사용자 신원 최종 검증
    - 계획서 흐름: 로그인 및 통신 시 OTP 번호와 신원을 최종 검증
    - 강의 자료 연계: 강의 자료의 'CRL / OCSP (인증서 폐기/실시간 유효성 상태 목록 조회)' 개념을 추가!

    - 가이드: pyotp 라이브러리로 유저가 입력한 6자리 OTP(user_code)가 맞는지 검사하고, 추가로 이 사용자의 인증서가 정지 상태가 아닌지 목록을 조회해서 최종 '신원 확인 승인'을 내려주는 코드 작성. OTP 검증과 CRL/OCSP 조회 모두 통과 시 '신원 확인 승인' 메시지 출력, 하나라도 실패 시 '신원 확인 거부' 메시지 출력하도록 구현 권유.
    """

    print(f"\n[4단계] 신원 확인: 입력된 OTP 번호('{user_code}') 검증 및 실시간 인증서 상태(CRL/OCSP) 최종 확인 중...")
    # 여기에 코드를 채워 넣을 공간
    return True


if __name__ == "__main__":
    print("=================================================================")
    print("   박민지, 조민정,정은미, 유채원, 김정서 팀의 보안 프로토콜 프로젝트 실행 시작! ")
    print("=================================================================")
    
    # [팀장 박민지]향후 이 위치에 팀원들이 완성한 함수들을 순서대로 실행시킬 예정입니다.
